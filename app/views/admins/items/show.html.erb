<div class="container">
  <div class="row">
    <div class="col-xs-8 col-xs-offset-1">
      <h4 style="margin-bottom: 100px;">商品詳細</h4>
      <div class="row">
        <div class="col-xs-4">
          <%= attachment_image_tag @item, :image, :fill, 250, 200, format: 'jpeg' %>
        </div>
        <div class="col-xs-8">
            <div class="col-xs-12" style="margin-bottom: 10px;">
              <strong class="col-xs-5">商品名</strong>
              <span class="col-xs-6"><%= @item.name %></span>
            </div>
            <div class="col-xs-12" style="margin-bottom: 10px;">
              <strong class="col-xs-5">商品説明</strong>
              <span class="col-xs-6"><%= @item.introduction %></span>
            </div>
            <div class="col-xs-12" style="margin-bottom: 10px;">
              <strong class="col-xs-5">ジャンル</strong>
              <span class="col-xs-6"><%= @item.genre.name %></span>
            </div>
            <div class="col-xs-12" style="margin-bottom: 10px;">
              <strong class="col-xs-5">税込価格（税抜価格）</strong>
              <!-- addTax(price)は、app/helpers/application_helper.rb にヘルパーメソッドとして定義しています-->
              <span class="col-xs-6" ><%= addTax(@item.price) %>（<%= @item.price %>）円</span>
            </div>
            <div class="col-xs-12" style="margin-bottom: 10px;">
              <strong class="col-xs-5">販売ステータス</strong>
              <span class="col-xs-6"><%= @item.is_active ? '販売中' : '販売停止中' %></span>
            </div>
          <div class="pull-right">
            <%= link_to "編集する", edit_admins_item_path(@item), class: "btn btn-primary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>